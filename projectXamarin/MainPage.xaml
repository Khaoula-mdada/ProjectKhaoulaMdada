<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
            xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
            xmlns:d="http://xamarin.com/schemas/2014/forms/design"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
            mc:Ignorable="d"
            x:Class="projectXamarin.MainPage">

    <ScrollView>
        <StackLayout HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
            <Entry Placeholder="Id country" Text="{Binding Querry}" Completed="Entry_Completed" />
            <Button Text="Get Corona Statistic" Command="{Binding GetCommand}" IsEnabled="{Binding IsNotBusy}"/>
            <ActivityIndicator IsVisible="{Binding IsBusy}" IsRunning="{Binding IsBusy}" VerticalOptions="Center"/>
            <Label Text="{Binding Intitule}" HorizontalTextAlignment="Center" />
            <Label Text="{Binding Code}" HorizontalTextAlignment="Center" />
          
        </StackLayout>
    </ScrollView>
</ContentPage>
